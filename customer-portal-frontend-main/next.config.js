// @ts-check

// const basePath = process.env.NEXT_APP_BASE_PATH || '/customer_portal';

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  basePath:'/customer_portal'
}

module.exports = nextConfig
